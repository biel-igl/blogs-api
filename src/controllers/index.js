const login = require('./login');
const userController = require('./user');
const categorieController = require('./categorie');

module.exports = {
    login,
    userController,
    categorieController,
};